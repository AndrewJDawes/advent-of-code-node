import { expect } from 'chai';
import { md5, hexadecimal } from './Common.js';
describe('Solution20164 Common', () => {
    describe('parseRoomInputParts', () => {
        it('throws when checksum too long', () => {
            expect(true).to.equal(true);
        });
    });
});
